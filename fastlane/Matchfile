git_url("https://github.com/skylineagle/chowder-certs.git")
app_identifier("com.sky.chowder")
username("sky") # Will be overridden by CI

# For CI, use environment variables:
# MATCH_PASSWORD - passphrase for encryption
# GIT_AUTHORIZATION - base64 encoded credentials if needed
